# M3-T9 实机验证清单（v1.0，2026-09-09）

> 任务：T9 端到端冒烟（§6.2 清单）+ NFR 抽测（N1–N4）→ 出口标准达成即 **v0.4.0 MVP**。
> 本文件为实机验证执行单。**所有 `flutter` 命令只能在你的正常网络机器上跑**——沙箱内 flutter CLI 因 flutter_tools 内部 stall 会挂死（见 `DSH_DESKTOP_ENV_STATUS.md` §7），沙箱仅能跑 `dart analyze` / `dart test`（已全部通过，见文末）。

---

## 0. 前置条件（一次性）

| 项 | 说明 |
|---|---|
| 取码 | 本地 `git fetch && git checkout -B feat/v0.4-m3 origin/feat/v0.4-m3`（远端 tip `4fb5dd9`） |
| 镜像变量 | `FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn` `PUB_HOSTED_URL=https://pub.flutter-io.cn`（PowerShell 跑 flutter 前 set） |
| 构建 | 功能验证用 `flutter run -d windows`；N1 计时在 release 产物上测：`flutter build windows --release` → `apps/desktop_flutter/build/windows/x64/runner/Release/dsh_desktop.exe` |
| 运行时 | 目标机预装 **Node.js ≥ dsh 要求**（引擎页 OQ3 会探测引导）；VC 运行时 DLL 由构建产物自带 |
| 后端 | 用 app 内「实例管理」自建 dsh 实例（T4 走 manager 守护拉起），无需外部另起 |
| 审批场景 | 需一个会触发审批的工具（如让 agent **写文件 / 执行命令**类工具），用于步骤 6 |

---

## 1. E2E 冒烟清单（8 步，Windows release 产物上执行）

| # | 步骤 | 预期 | 实测结果 | 证据 |
|---|---|---|---|---|
| 1 | 启动 app | 首窗 < 2s（N1）；引擎页探测到 node/dsh | □PASS □FAIL | 计时/截图 |
| 2 | 创建实例（端口自动 0） | starting→running；面板状态徽标更新 | □PASS □FAIL | 截图 |
| 3 | 切到对话页 | 自动注入活动端点并 streaming 连接（T5） | □PASS □FAIL | 截图 |
| 4 | 新建会话并发消息 | 首个 text-delta 上屏 < 1s（N2） | □PASS □FAIL | 计时 |
| 5 | 长回答流式渲染 | 增量流畅、代码块高亮、滚动锚定不跳动 | □PASS □FAIL | 录屏/截图 |
| 6 | 触发一次需审批的工具 | 审批浮层出现 → 允许 → run 继续 | □PASS □FAIL | 截图+操作 |
| 7 | `kill` dsh 进程 | 面板显示守护重拉；chat 断线提示 → 自动恢复 streaming | □PASS □FAIL | 操作记录 |
| 8 | 最小化到托盘→全部停止→退出 | 实例 stopped、进程退出干净 | □PASS □FAIL | 任务管理器核对 |

> 步骤 7 的守护重拉：在任务管理器结束 `dsh`/`node` 子进程，观察 app 是否自动 restart（F2  guardian 1s→30s 退避、≤5s 重拉）。

---

## 2. NFR 抽测方法

| 编号 | 指标 | 测量方法 | 达标线 | 实测 |
|---|---|---|---|---|
| N1 冷启动 | release 首窗出现 | 双击 exe → 秒表到首窗可交互 | < 2s | _____ s |
| N2 首帧 | 本机实例发消息→首 delta | 点发送 → 首 text-delta 上屏秒表 | < 1s | _____ s |
| N3 大会话 | 千条消息滚动流畅 | 长对话或灌入历史后上下滚动，观察是否掉帧/卡顿 | 无明显卡顿 | □PASS |
| N4 内存 | UI 进程稳态 | 对话空闲 1 分钟后看 `dsh_desktop.exe` 工作集（不含 dsh 子进程） | ≤ 300MB | _____ MB |

---

## 3. 结果记录（填后回报）

```
E2E: 1□ 2□ 3□ 4□ 5□ 6□ 7□ 8□   （□=PASS/FAIL，FAIL 标步骤号）
NFR: N1=____s  N2=____s  N3=□  N4=____MB
异常步骤: ____________
```

---

## 4. 现象回报模板（贴给我即可，据实修）

```
【环境】flutter _____ / 机器 _____（Win11?）/ Node _____
【卡点/报错】<粘贴控制台日志或截图路径>
【步骤】卡在 E2E 第 __ 步 / NFR __ 不达标
【现象】<描述：UI 卡死/无响应/审批不出现/断线不恢复/...>
【截图/录屏】<路径或描述>
```

---

## 5. 沙箱可跑门禁（已复核，无需你重跑）

| 门禁 | 命令 | 结果 @ `4fb5dd9` |
|---|---|---|
| 静态分析（Flutter 应用） | `dart analyze apps/desktop_flutter` | ✅ No issues found |
| 静态分析（client_dart） | `dart analyze packages/client_dart` | ✅ No issues found |
| 契约/连接/聚合单测 | `dart test packages/client_dart` | ✅ 6/6 passed |

> 注：`flutter test`（widget 冒烟 1/1）在沙箱因 flutter_tester 本地 WS 被代理劫持而无法跑；你本机 `unset` 代理后 `flutter test` 即可。源码自 T8(`2cfc877`) 起无变更，本次 T9 仅新增本文档，无源码改动。

---

## 6. 出口判定

- E2E 8 步全 PASS + N1/N2 达标 → **v0.4.0 MVP 出口达成**。
- 任一 FAIL → 把第 4 节模板回报给我，定位修复（fix 提交）→ 重测，直至全绿。
- N3/N4 为抽样项：不达标记录数值，评估是否进 M4 优化（非 MVP 阻塞，但需留痕）。
