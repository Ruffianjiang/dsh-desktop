{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window of the DSH desktop shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
